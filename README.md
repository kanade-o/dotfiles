# dotfiles

my dotfiles

## Requirements

- terminal: wezterm
- prompt theme: starfish
- font: `brew install font-moralerspace`

## How to add a new file

1. Move file or dir to `~/.dotfiles/`
2. Create symbolic link

```
ln -s ~/.dotfiles/.hoge ~/.hoge
```

3. Git push
